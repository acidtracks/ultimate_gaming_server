#!/bin/bash
# Restaure le VT sauvegardé et supprime le flag de session de jeu.
DIR="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/ultimate_gaming_server"
VT=$(cat "$DIR/saved-vt" 2>/dev/null | tr -d 'tty')
[ -n "$VT" ] && sudo chvt "$VT"
rm -f "$DIR/gaming-session"
