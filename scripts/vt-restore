#!/bin/bash
# Fin de session de jeu : repasse sur VT1 (Hyprland) et retire le flag.
# Le dpms on sera appelé après, donc le switch est invisible.
sudo chvt 1
rm -f "${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/ultimate_gaming_server/gaming-session"
