#!/bin/bash
# Sauvegarde le VT actif, marque la session de jeu, et bascule sur le VT Xorg (VT8).
DIR="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/ultimate_gaming_server"
mkdir -p "$DIR"
cat /sys/class/tty/tty0/active > "$DIR/saved-vt"
touch "$DIR/gaming-session"
sudo chvt 8
