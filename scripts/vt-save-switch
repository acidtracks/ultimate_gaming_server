#!/bin/bash
# Début de session de jeu : bascule sur VT8 (X :1) et pose le flag.
# Le dpms off a déjà été appelé avant, donc le switch est invisible.
DIR="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/ultimate_gaming_server"
mkdir -p "$DIR"
touch "$DIR/gaming-session"
sudo chvt 8
